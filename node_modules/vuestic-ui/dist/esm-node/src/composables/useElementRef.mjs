import { shallowRef, computed, unref } from "vue";
const unrefElement = (el) => {
  var _a;
  const e = unref(el);
  return (_a = e == null ? void 0 : e.$el) != null ? _a : e;
};
const useElementRef = () => {
  const el = shallowRef();
  return computed({
    get() {
      return unrefElement(el);
    },
    set(node) {
      el.value = node;
    }
  });
};
export { useElementRef as u };
//# sourceMappingURL=useElementRef.mjs.map
