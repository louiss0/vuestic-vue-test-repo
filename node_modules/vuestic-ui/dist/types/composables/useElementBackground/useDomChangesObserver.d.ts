import { Ref } from 'vue';
declare type Handler<K = any, V = any> = {
    cb: ((cache: Map<K, V>) => void);
    el: Ref<HTMLElement | undefined>;
};
/** Creates on global watched for dom changes */
export declare const useDomChangesObserver: <K, V>(cb: (cache: Map<K, V>) => void, el: Ref<HTMLElement | undefined>) => void;
export {};
